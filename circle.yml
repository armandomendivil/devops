version: 2
jobs:
  build:
    working_directory: ~/app
    docker:
      - image: platzi
      - entrypoint: .
    steps:
      - run:
          name: "Checking Versions"
          command: |
            node --version
            npm --version
